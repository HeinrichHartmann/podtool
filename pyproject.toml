[project]
name = "podtool"
version = "1.0.0"
description = "Audio processing and transcript refinement utility"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project.scripts]
podtool = "podtool.main:cli"